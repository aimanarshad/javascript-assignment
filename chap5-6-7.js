// Declare variables
let a = 2; // Initial value of 'a'
let b = a++ + ++a - --a + a--; // Evaluate expression for 'b'

// Display the results in alerts
alert(`Value of variable 'a': ${a}`);
alert(`Value of variable 'b': ${b}`);




